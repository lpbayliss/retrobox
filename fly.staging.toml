# fly.toml file generated for retrobox-staging on 2022-09-13T01:13:21+10:00

app = "retrobox-staging"
kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[env]
APP_URL = "staging.retrobox.app"
NEXTAUTH_URL = "https://staging.retrobox.app"
NODE_ENV = "production"
EMAIL_FROM = "noreply-staging@retrobox.app"
LOG_LEVEL="debug"
NEXT_PUBLIC_UNLEASH_PROXY_URL="https://staging.unleash-proxy.retrobox.app/proxy"
NEXT_PUBLIC_UNLEASH_CLIENT_KEY="staging-proxy"
NEXT_PUBLIC_UNLEASH_REFRESH_INTERVAL=15
NEXT_PUBLIC_UNLEASH_APP_NAME="retrobox-staging"
NEXT_PUBLIC_UNLEASH_ENVIRONMENT="development"

[build]
build-target = "runner"

[deploy]
release_command = "npx prisma migrate deploy"

[experimental]
allowed_public_ports = []
auto_rollback = true

[[services]]
http_checks = []
internal_port = 3000
processes = ["app"]
protocol = "tcp"
script_checks = []
[services.concurrency]
hard_limit = 25
soft_limit = 20
type = "connections"

[[services.ports]]
force_https = true
handlers = ["http"]
port = 80

[[services.ports]]
handlers = ["tls", "http"]
port = 443

[[services.tcp_checks]]
grace_period = "1s"
interval = "15s"
restart_limit = 0
timeout = "2s"
